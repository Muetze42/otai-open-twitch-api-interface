/* Pseudo file for IDE */

import Vue from 'vue'

Vue.mixin({
    // data: {
    //     config: {
    //         app_name: String,
    //         app_name_short: String,
    //         theme: {
    //             default: String,
    //             themes: [this.map(), this.each()],
    //             prefix: String,
    //         },
    //         scopes: {
    //             api: [this.map(), this.each()],
    //             chat: [this.map(), this.each()],
    //         }
    //     },
    // },
})
